using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriangulationGraph
{

   void CreateSuperTriangle(List<Vector2Int> verts)
    {
        //The 'verts' represent rooms which ther super traingle needs to to enclose. 
        //Create a triangle which has a point (0,0), (gridsize + 1, 0), (1/2(gridsize), gridsize + 1)
    }
}
